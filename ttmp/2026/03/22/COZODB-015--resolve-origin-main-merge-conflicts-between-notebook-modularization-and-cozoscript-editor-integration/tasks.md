# Tasks

## TODO

- [x] Resolve `NotebookCellCard` semantically by keeping the container/view split and porting `CozoScriptEditor` plus ANSI stripping
- [x] Resolve `NotebookPage` semantically by keeping the thin page component and porting keyboard fixes into `useNotebookPageController`
- [x] Reconcile `notebook.css` and `package.json`, then regenerate `package-lock.json`
- [x] Union `ttmp/vocabulary.yaml` and keep the incoming editor ticket docs
- [x] Validate with frontend test, lint, typecheck, build, Storybook build, and a manual CodeMirror keyboard smoke test
