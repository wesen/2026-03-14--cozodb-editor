# Tasks

## TODO

- [ ] Resolve `NotebookCellCard` semantically by keeping the container/view split and porting `CozoScriptEditor` plus ANSI stripping
- [ ] Resolve `NotebookPage` semantically by keeping the thin page component and porting keyboard fixes into `useNotebookPageController`
- [ ] Reconcile `notebook.css` and `package.json`, then regenerate `package-lock.json`
- [ ] Union `ttmp/vocabulary.yaml` and keep the incoming editor ticket docs
- [ ] Validate with frontend test, lint, typecheck, build, Storybook build, and a manual CodeMirror keyboard smoke test
